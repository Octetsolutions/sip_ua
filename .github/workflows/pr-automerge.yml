# pr-automerge caller — Octetsolutions standard (thin shim; logic lives in
# Octetsolutions/.github reusable @v2). workflow_run is the load-bearing
# post-CI trigger: check_suite events from GitHub Actions suites do NOT fire
# workflows, so without it labelled PRs only merged on the (throttled) cron.
name: pr-automerge

on:
  pull_request:
    types: [opened, labeled, synchronize, reopened, ready_for_review]
  check_suite:
    types: [completed]
  workflow_run:
    workflows: ["ci", "CI", "Dart", "dart"]
    types: [completed]
  schedule:
  - cron: '17,47 * * * *'
  workflow_dispatch: {}

permissions:
  contents: write
  pull-requests: write
  checks: read
  actions: read

jobs:
  automerge:
    uses: Octet-Public/octet-workflows/.github/workflows/pr-automerge.yml@ebd81fe56b97b70d11321cac4cfe74b9d66a1c88  # no-op merge guard (post-v2.3.0)
    secrets:
      AUTOMERGE_APP_ID: ${{ secrets.AUTOMERGE_APP_ID }}
      AUTOMERGE_APP_PRIVATE_KEY: ${{ secrets.AUTOMERGE_APP_PRIVATE_KEY }}
    with:
      required_checks: '[]'
      merge_method: squash
      base_ci_workflow: 'dart.yml'
